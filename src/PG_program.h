#include "HC_struct.h"

/* program: Compress input and write file out if specified */
Data **program(Files *io, HC_HuffmanNode **tree, Data **map);

/* PG_map_init: Initialise array for char map */
Data **PG_map_init(void);
